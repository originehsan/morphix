name: morphix_example
description: Example app for morphix.
version: 1.0.0+1
publish_to: none

environment:
  sdk: '^3.8.1'

dependencies:
  flutter:
    sdk: flutter
  morphix:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
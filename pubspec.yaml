name: morphix
description: A premium animated async-button Flutter package. Handles the full async button lifecycle — idle, loading, success, error — with spring physics, particle burst, neon glow, and gradient styles.
version: 1.0.0
repository: https://github.com/originehsan/Morphix_Package
issue_tracker: https://github.com/originehsan/Morphix_Package/issues

environment:
  sdk: '^3.8.1'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
name: morphix
description: A premium animated async-button Flutter package.
version: 1.0.0

environment:
  sdk: '^3.8.1'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: